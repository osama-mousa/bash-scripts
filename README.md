To Use Firefox with Tor proxy
use:
```bash
bash <(curl -s https://raw.githubusercontent.com/osama-mousa/bash-scripts/main/proxy.sh)
```

To use Sqlmap 
use: 
```bash
chmod +x sqlmap_scan.sh
./sqlmap_scan.sh
```
